{% extends 'base.html.twig'%}

{% block title %}{{ ticket.titre }}{% endblock %}

{% block body %}
<div class="col-md-6">
	<article>
		
			<h2>{{ ticket.titre }}</h2>
			<div class="metadata"> Ecrit le {{ ticket.createdAt | date('d/m/Y') }} à {{ ticket.createdAt| date('H:i') }}</div>
			<div class="contenu">
				<img src="{{ ticket.image }}" alt="">
				{{ ticket.contenu | raw }}
			</div>
		
	</article>
</div>
<div class="col-md-6">
	<div class="contact">
		
		
	</div>
</div>


{% endblock %}